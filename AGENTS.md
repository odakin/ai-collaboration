# Agent instructions

Before working in this repository, read `CLAUDE.md`, `SESSION.md`, and the
task-relevant part of `DESIGN.md`. Follow the common repository contract in
[`claude-config/CONVENTIONS.md`](../claude-config/CONVENTIONS.md), while keeping
this public layer-1 repository free of private project data and machine-local
paths.

This file is the thin Codex discovery entry point. Conventions live under
`conventions/`, implementation decisions live in `DESIGN.md`, and current work
state lives in `SESSION.md`; do not duplicate their payload here.
